우리는 화재 상황, 비화재 상황, 정상 상황을 구분하는 분류기를 만들고자 해. 입력 데이터는 시계열 데이터야. LDT_processed 폴더 안에 0_normal, 1_fire, 2_nofire라는 이름의 폴어들이 존재해. 각 폴더 안에는 여러 개의 csv 파일들이 존재하는데, 1_fire에 있다고 해서 그 안에 있는 csv 파일의 모든 타임 스탬프가 화재로 라벨링되는 것은 아니야. 단지, 0_normal, 1_fire, 2_nofire로 구분해 놓은 것은 나중에 training/valiation/test 셋 분할을 할 때, 각 폴더에서 샘플들이 일정한 비율이 유지된 채로 분할되었으면 좋겠어서 구분을 해 놓은 거야. 예를 들어, 8:1:1로 분할한다면, 각 폴더에서 8:1:1을 유지하면서 분할되도록. 하나의 csv 파일은 하나의 시나리오에 대응이 되고, 총 9개의 열 (MAC, INST_DT, TEMPO, FLAME, SMOKE, CODE, STATUS, LABEL, CATEGORY)로 구성되어 있어. MAC은 화재감지기의 MAC 주소이고, INST_DT는 시간이고, TEMPO는 온도 센서 값, FLAME은 불꽃 센서 값, SMOKE는 연기 센서 값이야. LABEL은 라벨이고 0일 때 정상, 1일 때 화재, 2일 때 비화재를 의미해. 우리는 TEMPO, FLAME, SMOKE를 입력 신호로 하여 LABEL을 예측하는 모델을 학습하고자 하고, 특정 시점에서 그 시점과 과거 9개의 시점의 데이터를 입력으로 하고자 해. 행렬로 표현했을 때 10 by 3이 되도록. 일단 MLP 모델과 LSTM 모델을 학습하고 결과를 비교해 줘. 이때 training/validation/test 분할은 8:1:1로 일단 진행해 줘.
